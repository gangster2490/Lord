{
  "name": "tiktok-shop-creator-proxy",
  "version": "1.0.0",
  "description": "Minimal CORS proxy – forwards Anthropic API calls from the browser",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": { "node": ">=18" }
}
